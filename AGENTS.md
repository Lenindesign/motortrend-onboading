# Project instructions

## Deployment

- Never publish this project through Sites.
- For an explicitly requested deployment, use the GitHub-connected Netlify workflow.
- Run `npm run deploy:netlify` to build and verify without publishing.
- Only run `npm run deploy:netlify -- --push` when the user explicitly asks to push/deploy.
