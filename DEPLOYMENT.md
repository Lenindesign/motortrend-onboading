# Deployment preference

Deploy this project through Netlify. Pushing the `main` branch to GitHub triggers the Netlify deployment.
Do not use OpenAI Sites for deployment unless the user explicitly changes this instruction.
